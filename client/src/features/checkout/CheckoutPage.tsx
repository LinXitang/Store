import { Typography } from "@mui/material";

export default function ChekoutPage() {
    return (
        <Typography variant='h3'>
            Only logged in users shoul be able to see this!
        </Typography>
    )
}